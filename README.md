# גיבוש — מערכת הערכת מלש״בים

React + Vite, client-side only (no backend, no data persistence).

## פיתוח

```bash
npm install
npm run dev
```

## בדיקות

```bash
npm run test
```

## בנייה

```bash
npm run build
```
